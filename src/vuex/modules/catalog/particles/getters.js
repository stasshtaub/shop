export default {
    PRODUCTS(state) {
        return state.products;
    },
    FILTERS(state) {
        return state.filters;
    },
    SEARCH_RESULT(state) {
        return state.searchResult;
    }
}